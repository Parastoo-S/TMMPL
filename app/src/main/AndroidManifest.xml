<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="segproject.tmmpl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PeopleActivity" />
         <activity android:name=".TaskPage"
             android:label="Tasks"/>
        <activity android:name=".AddTask" />
        <activity android:name=".EditTask" />
        <activity android:name=".PeopleTaskList"/>
        <activity android:name=".ViewTask"></activity>

       
        <activity
            android:name=".Shopping"
            android:label="Shopping"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity android:name=".NewQuickAccess" />
        <activity android:name=".QuickAccess" />

        <activity android:name=".AddTask" />
        <activity android:name=".EditTask" />





        <activity android:name=".UserLogin"
            android:theme="@style/AppTheme.NoActionBar"/>


        <activity
            android:name=".TaskPage"
            android:label="Tasks"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>